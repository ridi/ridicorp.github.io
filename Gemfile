source 'https://rubygems.org'
gem 'github-pages'

group :jekyll_plugins do
  # paginate 만 포함하면 버그 있어서 안댐
  gem 'octopress-hooks', git: 'https://github.com/octopress/hooks.git'
  gem 'octopress-paginate'
end
